// Generated by CoffeeScript 1.4.0
var $;

$ = Zepto;

$(function() {
  var COLOURS, COLUMNS, ROWS, ball, board, colour, column, columnIndex, rowIndex, _i, _j, _ref, _ref1, _results;
  board = $('#sausis .board');
  COLUMNS = 5;
  ROWS = 5;
  COLOURS = ['red', 'blue', 'green'];
  _results = [];
  for (columnIndex = _i = 0, _ref = COLUMNS - 1; 0 <= _ref ? _i <= _ref : _i >= _ref; columnIndex = 0 <= _ref ? ++_i : --_i) {
    column = $('<div />');
    column.addClass('column');
    column.data('x', columnIndex);
    for (rowIndex = _j = 0, _ref1 = ROWS - 1; 0 <= _ref1 ? _j <= _ref1 : _j >= _ref1; rowIndex = 0 <= _ref1 ? ++_j : --_j) {
      colour = COLOURS[Math.floor(Math.random() * COLOURS.length)];
      ball = $('<div />');
      ball.addClass('ball');
      ball.addClass(colour);
      ball.data('x', columnIndex);
      ball.data('y', rowIndex);
      column.append(ball);
    }
    _results.push(board.append(column));
  }
  return _results;
});
